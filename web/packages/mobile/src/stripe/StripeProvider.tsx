// Re-export for consistent import paths across the app.
// The provider itself is configured in app/_layout.tsx with the publishable key from app.config.js.
export { StripeProvider } from '@stripe/stripe-react-native';
